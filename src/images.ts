// Centralized image imports per decision #21. Add named exports as image
// assets are introduced. Empty for now — the home page renders icons as
// inline SVG components and articles fetch their cover images at runtime
// from the backend.
export {};
